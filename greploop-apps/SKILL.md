---
name: greploop-apps
description: Read Greptile Apps feedback for a large preview-to-main promotion PR when explicitly requested. This helper is read-only.
license: MIT
---

# Large promotion review helper

This is the large-PR variant of `greploop`. It only reads existing Greptile Apps
feedback for the promotion PR and produces a local, redacted summary. Never tag the
app, edit a PR, resolve a thread, commit, or push unless the user separately asks.
